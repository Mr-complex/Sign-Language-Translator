[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "14"

[dev]
  command = "npm run start"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
